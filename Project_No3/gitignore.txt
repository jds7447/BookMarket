.gitignore 파일은 프로젝트 최상위 위치에 존재해야한다

패턴을 활용하여 git이 untracked할 파일 또는 디렉토리등을 정의하여 파일을 생성한다
 
'#'로 시작하는 라인은 무시한다
표준 Glob 패턴을 사용한다
슬래시(/)로 시작하면 하위 디렉터리에 적용되지(recursivity) 않는다
디렉터리는 슬래시(/)를 끝에 사용하는 것으로 표현한다
느낌표(!)로 시작하는 패턴의 파일은 무시하지 않는다