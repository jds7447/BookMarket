<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.market.mapper.AttachMapper">

	<!-- 이미지 데이터 반환 -->
	<select id="getAttachList" resultType="com.market.model.AttachImageVO">
		select * from book_image where bookId = #{bookId}
	</select>

</mapper>